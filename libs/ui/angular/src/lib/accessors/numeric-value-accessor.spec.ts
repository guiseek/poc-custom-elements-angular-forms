import { NumericValueAccessor } from './numeric-value-accessor';

describe('NumericValueAccessor', () => {
  it('should create an instance', () => {
    expect(new NumericValueAccessor()).toBeTruthy();
  });
});
