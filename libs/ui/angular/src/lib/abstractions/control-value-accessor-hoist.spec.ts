import { ControlValueAccessorHoist } from './control-value-accessor-hoist';

describe('ControlValueAccessorHoist', () => {
  it('should create an instance', () => {
    expect(new ControlValueAccessorHoist()).toBeTruthy();
  });
});
