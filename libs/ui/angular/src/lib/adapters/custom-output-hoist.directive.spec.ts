import { CustomOutputHoistDirective } from './custom-output-hoist.directive';



describe('CustomOutputHoistDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomOutputHoistDirective(Object());
    expect(directive).toBeTruthy();
  });
});
