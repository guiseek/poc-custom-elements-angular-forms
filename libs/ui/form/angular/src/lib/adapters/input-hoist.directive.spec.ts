import { InputHoistDirective } from './input-hoist.directive';

describe('InputHoistDirective', () => {
  it('should create an instance', () => {
    const directive = new InputHoistDirective();
    expect(directive).toBeTruthy();
  });
});
